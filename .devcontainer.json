{
    "image": "ubuntu:16.04",
    "forwardPorts": [
        3000
    ],
    "extensions": [
        "esbenp.prettier-vscode",
        "ms-vscode.cpptools"
    ],
    "onCreateCommand": "apt update && apt install -y build-essential && apt install -y git"
}